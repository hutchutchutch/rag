{
  "name": "rag-monorepo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "packageManager": "pnpm@8.10.0",
  "scripts": {
    "dev": "turbo run dev",
    "dev:frontend": "turbo run dev --filter=frontend",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "api:test": "pnpm --filter @apps/backend test:api"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  }
}